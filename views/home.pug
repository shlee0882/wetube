extends layouts/main.pug
include mixins/videoBlock

block content
    .videos
        each item in videos
            +videoBlock({
                id: item.id,
                videoFile: item.fileUrl,
                title: item.title,
                views: item.views
            })